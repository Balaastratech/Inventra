# Inventra — Agentic Stockout Resolution System
#
# Pinned to the versions this submission was developed and verified against
# (Python 3.13.7, Windows). Verified complete against actual imports across
# submission/, tools/, domain/, database/ on 2026-09-13 (fastapi, uvicorn,
# and pandas were previously missing from this file despite being real,
# unconditional imports — added below).
#
# Install:  pip install -r requirements.txt

# --- Core: graph, state, checkpointing ---
langgraph==1.2.8
langgraph-checkpoint-sqlite==3.1.1

# --- Typed contracts (every agent output is Pydantic-validated) ---
pydantic==2.13.4

# --- LLM providers. Only ONE is required at runtime; MODEL_PROVIDER picks it.
#     With none configured the system falls back to deterministic stub agents
#     and still runs end to end at zero LLM cost. Default is MODEL_PROVIDER=gemini.
langchain-core==1.4.8
google-genai==2.17.0            # MODEL_PROVIDER=vertex  (Vertex AI via ADC)
langchain-google-genai==4.2.6   # MODEL_PROVIDER=gemini  (AI Studio API key) -- the default
langchain-openai==1.3.3         # MODEL_PROVIDER=openai

# --- Operator UI (Streamlit approval screens + Data Console) ---
streamlit==1.58.0
streamlit-aggrid==1.2.1.post2
pandas==2.3.3                   # used by data_console.py's table editor

# --- Email approval server (submission/graph/approval_server.py) ---
fastapi==0.141.1
uvicorn==0.52.1

# --- Config ---
python-dotenv==1.2.2

# --- Tests (optional -- only needed if you run submission/tests/) ---
pytest==9.1.1

# --- Optional: LangSmith tracing.
#     agents/llm.py calls langsmith.traceable only when LANGCHAIN_TRACING_V2
#     is set AND the package imports; it is a no-op otherwise, so the suite
#     runs without a LangSmith account. Uncomment to enable.
# langsmith==0.4.42
